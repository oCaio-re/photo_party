# Capability-Based Admin Access Over User Accounts

Host administration of events is secured via unique capability tokens (Host Keys / secret management URLs and optional PIN) rather than traditional user accounts with email/password or OAuth. This eliminates third-party auth dependencies (such as transactional email providers or OAuth consent verification), allowing instant event creation with zero onboarding friction while maintaining strict isolated access control per event.
